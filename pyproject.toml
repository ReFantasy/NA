[project]
name = "na"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jax>=0.9.2",
    "loguru>=0.7.3",
]

[tool.setuptools]
packages = ["NumercialAnalysis"]
